#include <iostream>

int main()
{
  double number;
  int n;
  std::cout << " The number of decimals required" << std::endl;
  std::cin >> n;
  int x = number;

  double step = 0.1;
  for (int i = 0, i < n; i++)
  {
    while (number)
  }
}